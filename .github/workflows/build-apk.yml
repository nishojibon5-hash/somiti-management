name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build:client

      - name: Initialize Capacitor
        run: |
          npx cap init "সমিতি ম্যানেজার" "com.somitimanager.app" --web-dir=dist

      - name: Add Android platform
        run: npx cap add android

      - name: Copy web assets
        run: npx cap copy android

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: somiti-manager-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

      - name: Create Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: সমিতি ম্যানেজার v1.0.${{ github.run_number }}
          body: |
            ## 📱 সমিতি ম্যানেজার Android অ্যাপ

            ### ✨ Features:
            - সদস্য ব্যবস্থাপনা
            - দৈনিক সংগ্রহ সিস্টেম
            - এডমিন প্যানেল
            - বাংলা ও ইংরেজি সাপোর্ট
            - অফলাইন কাজ করে

            ### 📋 Installation:
            1. Download the APK file below
            2. Enable "Unknown Sources" in phone settings
            3. Install the APK
            4. Open "সমিতি ম্যানেজার" app

            **App Size:** ~15 MB  
            **Minimum Android:** 5.0+
          files: |
            android/app/build/outputs/apk/debug/app-debug.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
